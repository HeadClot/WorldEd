/**
 * Re-export of the shared inline rename input. Kept so existing outliner import
 * paths continue to work without changing outliner visuals or call sites.
 */
export { InlineRenameInput, type RenameCancelCallback, type RenameConfirmCallback } from '../inline_rename_input.js';
