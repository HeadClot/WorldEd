/** Number of float components stored per vertex position (x, y, z). */
export const MESH_VERTEX_POSITION_STRIDE = 3;

/**
 * Maximum half-edges walked for a single face before treating the loop as
 * broken.
 */
export const MESH_FACE_WALK_SAFETY_LIMIT = 65536;
