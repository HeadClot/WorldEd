/** Screen-space vertex pick radius in CSS pixels (Blender-like generosity). */
export const EDIT_COMPONENT_VERTEX_PICK_RADIUS_PX = 72;

/** Screen-space edge pick radius in CSS pixels (Blender-like generosity). */
export const EDIT_COMPONENT_EDGE_PICK_RADIUS_PX = 56;
